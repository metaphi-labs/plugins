# csharp-lsp

C# code intelligence through `csharp-ls`.

Install the server: `dotnet tool install --global csharp-ls`.

Then `hum plugin install csharp-lsp@metaphi` (Humboldt) or `/plugin install csharp-lsp@metaphi` (Claude Code).
