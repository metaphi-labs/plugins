# kotlin-lsp

Kotlin code intelligence through `kotlin-language-server`.

Install the server: `brew install kotlin-language-server`.

Then `hum plugin install kotlin-lsp@metaphi` (Humboldt) or `/plugin install kotlin-lsp@metaphi` (Claude Code).
