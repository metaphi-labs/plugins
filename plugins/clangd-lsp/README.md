# clangd-lsp

C and C++ code intelligence through `clangd`.

Install the server: `apt install clangd, or brew install llvm`.

Then `hum plugin install clangd-lsp@metaphi` (Humboldt) or `/plugin install clangd-lsp@metaphi` (Claude Code).
