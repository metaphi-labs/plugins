# rust-analyzer-lsp

Rust code intelligence through `rust-analyzer`.

Install the server: `rustup component add rust-analyzer`.

Then `hum plugin install rust-analyzer-lsp@metaphi` (Humboldt) or `/plugin install rust-analyzer-lsp@metaphi` (Claude Code).
