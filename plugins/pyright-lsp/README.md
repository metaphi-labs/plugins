# pyright-lsp

Python code intelligence through `pyright-langserver`.

Install the server: `npm i -g pyright`.

Then `hum plugin install pyright-lsp@metaphi` (Humboldt) or `/plugin install pyright-lsp@metaphi` (Claude Code).
