# typescript-lsp

TypeScript and JavaScript code intelligence through `typescript-language-server`.

Install the server: `npm i -g typescript typescript-language-server`.

Then `hum plugin install typescript-lsp@metaphi` (Humboldt) or `/plugin install typescript-lsp@metaphi` (Claude Code).
