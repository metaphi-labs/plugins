# php-lsp

PHP code intelligence through `intelephense`.

Install the server: `npm i -g intelephense`.

Then `hum plugin install php-lsp@metaphi` (Humboldt) or `/plugin install php-lsp@metaphi` (Claude Code).
