# lua-lsp

Lua code intelligence through `lua-language-server`.

Install the server: `brew install lua-language-server`.

Then `hum plugin install lua-lsp@metaphi` (Humboldt) or `/plugin install lua-lsp@metaphi` (Claude Code).
